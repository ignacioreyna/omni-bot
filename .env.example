# Server
PORT=3000
SESSION_SECRET=change_this_to_a_random_string

# Paths - comma-separated list of allowed working directories
# Supports: ~, $HOME, and other environment variables
ALLOWED_DIRECTORIES=$HOME/GIT,$HOME/.claude,/tmp
DATABASE_PATH=./data/omni-bot.db

# Optional
MAX_CONCURRENT_SESSIONS=5

# Auth Mode: "tailscale" (no auth, network security) or "cloudflare" (CF Access JWT)
AUTH_MODE=tailscale

# Cloudflare Access (required if AUTH_MODE=cloudflare)
# CF_ACCESS_TEAM_DOMAIN=yourteam.cloudflareaccess.com
# CF_ACCESS_AUD=your_application_audience_tag

# Wake Server
OMNI_BOT_PORT=3001
OMNI_BOT_LOG_PATH=/tmp/omni-bot.log
